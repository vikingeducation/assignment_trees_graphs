class BinaryTree
  Node = Struct.new(:data, :left, :right)

  def initialize
    @root = nil
  end

  # inserts an array of values into the BinaryTree.
  # assumes the input is an array of non-repeating integers.
  def insert(values)
  end

  # prints out the BinaryTree
  def render
  end
end
